public class MovementInteraction {
    /*
    Each robot has a priority or initiative
    Robot move in order of priority inside a round
          in a encounter in the same case
              -> If weight of robots is roughly equal
                  -> [Total weight > 1200]
                      -> Destroy both robots and create an obstacle
                  -> [Total wieght < 1200]
              -> If  weight of robot largely unequal
                  











    */
}
